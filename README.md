# 👩‍🍳 منصة مطبخ سارة للأكل البيتي (Sara's Home Kitchen)

منصة ويب متكاملة لإدارة وطلب الطعام البيتي، مصممة لتوفير تجربة مستخدم سلسة للعملاء، وأدوات إدارة قوية للمطبخ والمندوبين.

## ✨ الميزات الرئيسية

### 📱 للعملاء
- **طلب سهل وسريع:** تصفح المنيو، إضافة للسلة، وتخصيص الوجبات.
- **نظام ولاء:** تسجيل حساب، حفظ عناوين، وجمع نقاط ولاء مع كل طلب.
- **مساعد ذكي (AI):** "الشيف سارة" للإجابة على الأسئلة واقتراح الوجبات (مدعوم بـ Gemini Flash).
- **تتبع لحظي:** متابعة حالة الطلب خطوة بخطوة دون تحديث الصفحة.
- **إعادة الطلب:** تكرار الطلبات السابقة بضغطة زر.

### 👨‍🍳 للمطبخ (Kitchen Dashboard)
- **شاشة عرض الطلبات (KDS):** عرض الطلبات الجديدة والجارية في الوقت الفعلي.
- **تنبيهات صوتية:** جرس تنبيه عند وصول طلب جديد.
- **طباعة الفواتير:** دعم الطباعة الحرارية لبونات الطلبات.
- **إدارة المخزون:** خصم الكميات تلقائياً وإيقاف الأصناف النافذة.

### 🛵 للمندوبين (Driver App)
- **قائمة التوصيل:** عرض الطلبات الجاهزة للتوصيل.
- **خرائط جوجل:** زر مباشر لفتح موقع العميل على الخريطة.
- **تحديث الحالة:** تحديث حالة الطلب إلى "تم التسليم" بضغطة زر.

### 🛡️ للإدارة والأمان
- **لوحة تحكم شاملة:** إدارة المنتجات، التصنيفات، والمستخدمين.
- **أمان عالي:** استخدام Supabase RLS (Row Level Security) لحماية البيانات.
- **أدوار المستخدمين:** نظام صلاحيات دقيق (Admin, Cook, Driver, Customer).

## 🚀 التقنيات المستخدمة
- **Frontend:** HTML5, CSS3, JavaScript (Vanilla).
- **Backend:** Supabase (Database, Auth, Realtime, Edge Functions).
- **AI:** Google Gemini 1.5 Flash.

## 🛠️ الإعداد والتشغيل (Setup)

1.  **استنساخ المستودع:**
    ```bash
    git clone https://github.com/your-username/sara-kitchen-platform.git
    cd sara-kitchen-platform
    ```

2.  **إعداد Supabase:**
    - قم بإنشاء مشروع جديد على Supabase.
    - نفذ ملفات SQL الموجودة في المجلد الرئيسي بالترتيب:
        1. `supabase_setup.sql`
        2. `supabase_security_fixes.sql`
        3. `phase1_schema_updates.sql`
        4. `phase2_loyalty_schema.sql`
        5. `phase3_inventory.sql`

3.  **إعداد متغيرات البيئة:**
    - قم بإنشاء ملف `config.js` (أو عدل الموجود) وأضف بيانات مشروعك:
    ```javascript
    window.APP_CONFIG = {
        supabase: {
            url: "YOUR_SUPABASE_URL",
            anonKey: "YOUR_SUPABASE_ANON_KEY"
        }
    };
    ```

4.  **نشر دالة الذكاء الاصطناعي (اختياري):**
    - تأكد من تثبيت Supabase CLI.
    - انشر الدالة: `supabase functions deploy ai-chat`
    - أضف مفتاح Gemini: `supabase secrets set GEMINI_API_KEY=your_key`

5.  **التشغيل:**
    - افتح `index.html` في المتصفح (يفضل استخدام Live Server).

## 📄 الهيكلية
- `/`: ملفات الواجهة الأمامية (HTML, JS, CSS).
- `/supabase`: ملفات إعداد قاعدة البيانات ودوال الـ Edge.

---
تم التطوير بواسطة [اسمك/فريقك]
# sara-kitchen-ASH
